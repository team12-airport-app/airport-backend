spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:${MYSQL_HOST:localhost}}:${DB_PORT:3307}/${DB_NAME:flight_management}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/St_Johns
    username: ${DB_USER:airport_app}
    password: ${DB_PASSWORD:AirportApp2025!}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8080